{
  "name": "cf-neptune",
  "version": "0.0.1",
  "description": "Algorithm visualization, runs on browser.",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "author": "Mgen (https://github.com/mgenware)",
  "homepage": "https://github.com/mgenware/cf-neptune",
  "bugs": "https://github.com/mgenware/cf-neptune/issues",
  "keywords": [
    "algorithm", "visualization"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mgenware/cf-neptune.git"
  },
  "license": "MIT",
  "scripts": {
    "test": "yarn run clean && yarn run compile && yarn run lint && yarn run t",
    "t": "jest",
    "build": "yarn lint && NODE_ENV=production rollup -c",
    "compile": "tsc",
    "dev": "yarn run clean && NODE_ENV=development rollup -c -w",
    "prepublishOnly": "yarn run test",
    "lint": "tslint --project tsconfig.json --config tslint.json",
    "clean": "rimraf dist"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "devDependencies": {
    "@types/jest": "^23.0.0",
    "@types/node": "^10.3.2",
    "jest": "^23.1.0",
    "rimraf": "^2.6.2",
    "rollup": "^0.60.1",
    "rollup-plugin-commonjs": "^9.1.3",
    "rollup-plugin-node-resolve": "^3.3.0",
    "rollup-plugin-replace": "^2.0.0",
    "rollup-plugin-typescript2": "^0.14.0",
    "rollup-plugin-uglify": "^4.0.0",
    "tslint": "^5.10.0",
    "typescript": "^2.9.1",
    "unique-temp-dir": "^1.0.0"
  },
  "dependencies": {
    "popmotion-pose": "^2.0.6"
  }
}
